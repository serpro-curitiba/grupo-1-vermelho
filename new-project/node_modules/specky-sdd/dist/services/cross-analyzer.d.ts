/**
 * CrossAnalyzer — Cross-artifact consistency analysis.
 * Checks alignment between SPECIFICATION.md, DESIGN.md, and TASKS.md.
 */
import type { FileManager } from "./file-manager.js";
import type { CrossAnalysisResult } from "../types.js";
export declare class CrossAnalyzer {
    private fileManager;
    constructor(fileManager: FileManager);
    analyze(featureDir: string): Promise<CrossAnalysisResult>;
    private extractIds;
    private extractAllRefs;
    private extractTaskIds;
    private generateTraceabilityDiagram;
    private safeRead;
}
//# sourceMappingURL=cross-analyzer.d.ts.map