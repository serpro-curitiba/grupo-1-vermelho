/**
 * GitManager — Branch naming and PR payload generation.
 * Does NOT execute git commands. Generates payloads for the AI client.
 */
import type { FileManager } from "./file-manager.js";
import type { BranchInfo, PrPayload } from "../types.js";
export declare class GitManager {
    private fileManager;
    constructor(fileManager: FileManager);
    generateBranchInfo(featureNumber: string, featureName: string, prefix?: string, baseBranch?: string): BranchInfo;
    generatePrPayload(featureDir: string, featureNumber: string, baseBranch: string, headBranch?: string): Promise<PrPayload>;
}
//# sourceMappingURL=git-manager.d.ts.map