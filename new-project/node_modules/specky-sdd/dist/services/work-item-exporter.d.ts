/**
 * WorkItemExporter — Transforms tasks into platform-specific work item payloads.
 * Supports GitHub Issues, Azure Boards, and Jira.
 */
import type { FileManager } from "./file-manager.js";
import type { WorkItemExportResult, WorkItemPayload, GitHubIssuePayload, AzureBoardsPayload, JiraPayload, WorkItemPlatform } from "../types.js";
export declare class WorkItemExporter {
    private fileManager;
    constructor(fileManager: FileManager);
    export(platform: WorkItemPlatform, _specDir: string, featureDir: string, _includeSubtasks: boolean, _options?: {
        project_key?: string;
        area_path?: string;
        iteration_path?: string;
    }): Promise<WorkItemExportResult>;
    toGitHubPayloads(items: WorkItemPayload[], featureNumber: string): GitHubIssuePayload[];
    toAzureBoardsPayloads(items: WorkItemPayload[], areaPath?: string, iterationPath?: string): AzureBoardsPayload[];
    toJiraPayloads(items: WorkItemPayload[], projectKey: string): JiraPayload[];
    private parseTasks;
    private getRoutingInstructions;
}
//# sourceMappingURL=work-item-exporter.d.ts.map