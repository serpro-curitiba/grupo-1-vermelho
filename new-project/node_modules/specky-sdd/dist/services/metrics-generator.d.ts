/**
 * MetricsGenerator — Generates an HTML metrics dashboard from spec artifacts.
 * Reads SPECIFICATION.md, ANALYSIS.md, VERIFICATION.md, CHECKLIST.md and
 * .sdd-state.json to produce a self-contained local HTML report.
 */
import type { FileManager } from "./file-manager.js";
export interface PhaseMetric {
    phase: string;
    status: string;
    started_at?: string;
    completed_at?: string;
    duration_minutes?: number;
}
export interface MetricsResult {
    feature_dir: string;
    feature_number: string;
    generated_at: string;
    requirements_count: number;
    acceptance_criteria_count: number;
    compliance_score: number;
    test_coverage_percent: number;
    tasks_total: number;
    tasks_verified: number;
    phases: PhaseMetric[];
    html_path: string;
    html_content: string;
}
export declare class MetricsGenerator {
    private fileManager;
    constructor(fileManager: FileManager);
    generateMetrics(featureDir: string, featureNumber: string): Promise<MetricsResult>;
    private safeRead;
    private readState;
    private countRequirements;
    private countAcceptanceCriteria;
    private extractComplianceScore;
    private extractVerificationStats;
    private extractChecklistPassRate;
    private extractPhaseMetrics;
    private renderHtml;
}
//# sourceMappingURL=metrics-generator.d.ts.map