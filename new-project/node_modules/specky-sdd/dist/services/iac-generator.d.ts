/**
 * IacGenerator — Generates Terraform/Bicep and Dockerfile from DESIGN.md.
 * Deterministic template-based generation.
 */
import type { FileManager } from "./file-manager.js";
import type { IacResult, IacValidationResult, DevEnvironmentResult, IacProvider, CloudProvider } from "../types.js";
export declare class IacGenerator {
    private fileManager;
    constructor(fileManager: FileManager);
    generateTerraform(featureDir: string, cloud: CloudProvider, modules?: string[]): Promise<IacResult>;
    generateValidationPayload(provider: IacProvider, cloud: CloudProvider, iacDir?: string): IacValidationResult;
    generateDockerfile(techStack: {
        language: string;
        framework?: string;
        runtime: string;
    }, includeCompose: boolean, multiStage: boolean): DevEnvironmentResult;
    generateDevcontainer(techStack: {
        language: string;
        framework?: string;
    }, features?: string[], extensions?: string[]): DevEnvironmentResult;
    private detectModules;
    private generateMainTf;
    private generateVariables;
    private generateVariablesTf;
    private generateOutputsTf;
}
//# sourceMappingURL=iac-generator.d.ts.map