/**
 * TestGenerator — Generates test stubs from SPECIFICATION.md and TASKS.md.
 * Reads acceptance criteria and produces framework-specific test files.
 */
import type { FileManager } from "./file-manager.js";
export type TestFramework = "vitest" | "jest" | "playwright" | "pytest" | "junit" | "xunit";
export interface TestStub {
    id: string;
    requirement_id: string;
    description: string;
    test_code: string;
}
export interface TestGenerationResult {
    framework: TestFramework;
    stubs: TestStub[];
    output_file: string;
    content: string;
    total_tests: number;
}
export declare class TestGenerator {
    private fileManager;
    constructor(fileManager: FileManager);
    generate(featureDir: string, framework: TestFramework, outputDir: string): Promise<TestGenerationResult>;
    extractAcceptanceCriteria(text: string): Array<{
        id: string;
        criterion: string;
    }>;
    extractRequirements(text: string): string[];
    buildStubs(criteria: Array<{
        id: string;
        criterion: string;
    }>, _reqs: string[], framework: TestFramework): TestStub[];
    private generateTestBody;
    private renderFile;
    private snakeCase;
    private camelCase;
    private pascalCase;
    private safeRead;
    /**
     * Verify test results against specification requirements.
     * Accepts a JSON string of test results and cross-references with requirement IDs.
     */
    verifyTestResults(featureDir: string, testResultsJson: string): Promise<TestVerificationResult>;
    private extractRequirementIds;
}
export interface TestVerificationResult {
    total_requirements: number;
    covered_requirements: string[];
    uncovered_requirements: string[];
    coverage_percentage: number;
    total_tests: number;
    passed_tests: number;
    failed_tests: number;
    traceability_matrix: Array<{
        requirement: string;
        tests: string[];
        status: string;
    }>;
    error?: string;
}
//# sourceMappingURL=test-generator.d.ts.map