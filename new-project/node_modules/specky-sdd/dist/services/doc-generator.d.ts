/**
 * DocGenerator — Auto-documentation generation.
 * Generates full docs, API docs, runbooks, and onboarding guides.
 */
import type { FileManager } from "./file-manager.js";
import type { StateMachine } from "./state-machine.js";
import type { DocumentationResult } from "../types.js";
export declare class DocGenerator {
    private fileManager;
    private stateMachine?;
    constructor(fileManager: FileManager, stateMachine?: StateMachine | undefined);
    generateFullDocs(featureDir: string, featureNumber: string): Promise<DocumentationResult>;
    generateApiDocs(featureDir: string, featureNumber: string): Promise<DocumentationResult>;
    generateRunbook(featureDir: string, featureNumber: string): Promise<DocumentationResult>;
    generateOnboarding(featureDir: string, featureNumber: string): Promise<DocumentationResult>;
    generateAllDocs(featureDir: string, featureNumber: string): Promise<{
        results: DocumentationResult[];
        total_generated: number;
        total_sections: number;
    }>;
    generateJourneyDocs(featureDir: string, featureNumber: string): Promise<DocumentationResult>;
    private summarize;
    private extractEndpoints;
    private safeRead;
}
//# sourceMappingURL=doc-generator.d.ts.map