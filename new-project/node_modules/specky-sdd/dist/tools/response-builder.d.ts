/**
 * ResponseBuilder — Enriches tool responses with phase context, educational content,
 * handoff information, and parallel execution hints.
 */
import { Phase } from "../constants.js";
import type { PhaseStatus } from "../types.js";
/**
 * Build an enriched tool response with phase context and educational content.
 */
export declare function buildToolResponse(toolName: string, result: Record<string, unknown>, currentPhase: Phase, phases: Record<Phase, PhaseStatus>, options?: {
    completedPhase?: Phase;
    nextPhase?: Phase;
    artifactsProduced?: string[];
    summaryOfWork?: string;
}): Record<string, unknown>;
/**
 * Convenience wrapper: loads state from StateMachine and enriches a tool response.
 * This is the primary function tools should call — handles state loading internally.
 */
export declare function enrichResponse(toolName: string, result: Record<string, unknown>, stateMachine: {
    loadState(specDir: string): Promise<{
        current_phase: Phase;
        phases: Record<Phase, PhaseStatus>;
    }>;
}, specDir: string, options?: {
    completedPhase?: Phase;
    nextPhase?: Phase;
    artifactsProduced?: string[];
    summaryOfWork?: string;
}): Promise<Record<string, unknown>>;
/**
 * Enrich a stateless tool response (tools without spec_dir or stateMachine).
 * Provides educational content without phase context.
 */
export declare function enrichStateless(toolName: string, result: Record<string, unknown>): Record<string, unknown>;
/**
 * Build a validation error response when a tool is called in the wrong phase.
 */
export declare function buildPhaseError(toolName: string, currentPhase: Phase, expectedPhases: Phase[], errorMessage: string): Record<string, unknown>;
//# sourceMappingURL=response-builder.d.ts.map