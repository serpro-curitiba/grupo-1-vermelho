/**
 * Merge a Specky server entry into the given MCP config file.
 * - VS Code mcp.json uses `{ "servers": { ... } }` (new schema) or
 *   `{ "mcpServers": { ... } }` (legacy). We write both to be safe.
 * - Claude .mcp.json uses `{ "mcpServers": { ... } }`.
 */
export declare function writeMcpRegistration(path: string, opts: {
    dryRun: boolean;
    serverName?: string;
    useVscodeSchema?: boolean;
}): {
    path: string;
    written: boolean;
    action: "created" | "merged" | "unchanged";
};
//# sourceMappingURL=mcp-writer.d.ts.map