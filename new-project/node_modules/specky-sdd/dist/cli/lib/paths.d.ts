/**
 * Resolve the package root by walking up from the compiled CLI location.
 * After build, this file lives at `<package>/dist/cli/lib/paths.js`.
 * Before build (dev), at `<package>/src/cli/lib/paths.ts`.
 */
export declare function packageRoot(): string;
export declare function sourcePaths(pkgRoot: string): {
    apmDir: string;
    agentsDir: string;
    promptsDir: string;
    skillsDir: string;
    hookScriptsDir: string;
    hooksManifest: string;
    instructionsDir: string;
    templatesDir: string;
    claudeHooksManifest: string;
    copilotHooksManifest: string;
    configYml: string;
    apmYml: string;
};
export interface Targets {
    claude: {
        root: string;
        agents: string;
        commands: string;
        skills: string;
        hooksScripts: string;
        rules: string;
        settingsJson: string;
    };
    copilot: {
        agents: string;
        prompts: string;
        skills: string;
        hooksRoot: string;
        hooksScripts: string;
        hooksManifest: string;
        instructions: string;
    };
    shared: {
        specky: string;
        specs: string;
        vscodeMcp: string;
        claudeMcp: string;
    };
}
export declare function targetPaths(workspace: string): Targets;
//# sourceMappingURL=paths.d.ts.map