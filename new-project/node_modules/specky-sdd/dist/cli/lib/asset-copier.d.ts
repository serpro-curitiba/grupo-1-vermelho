import { basename } from "node:path";
import type { Targets } from "./paths.js";
export interface CopyResult {
    written: string[];
    skipped: string[];
}
export interface CopyOptions {
    force: boolean;
    dryRun: boolean;
}
/**
 * Copy assets to Claude Code `.claude/` layout.
 */
export declare function copyToClaude(pkgRoot: string, targets: Targets, opts: CopyOptions): CopyResult;
/**
 * Copy assets to GitHub Copilot `.github/` layout.
 */
export declare function copyToCopilot(pkgRoot: string, targets: Targets, opts: CopyOptions): CopyResult;
/**
 * Compute SHA256 of every copied file so `specky doctor` can detect drift.
 */
export declare function hashFile(path: string): string;
export declare function writeInstallLock(targets: Targets, copied: CopyResult[], version: string, opts: CopyOptions): string;
export declare function summarizeCopy(name: string, r: CopyResult): string;
export { basename };
//# sourceMappingURL=asset-copier.d.ts.map