import { resolve } from "node:path";
import type { Targets } from "./paths.js";
interface HookEntry {
    type: string;
    command: string;
    timeout?: number;
}
interface HookGroup {
    matcher: string;
    hooks: HookEntry[];
}
interface HooksSection {
    [event: string]: HookGroup[];
}
/** Tools that Specky agents need pre-authorized (avoids per-invocation prompts). */
export declare const SPECKY_REQUIRED_ALLOWS: string[];
export interface MergeResult {
    written: boolean;
    path: string;
    addedEvents: string[];
    addedPermissions: number;
}
export declare function mergeClaudeHooks(targets: Targets, claudeHooks: HooksSection, opts: {
    dryRun: boolean;
}): MergeResult;
export declare function loadClaudeHooksManifest(manifestPath: string): HooksSection;
export { resolve };
//# sourceMappingURL=settings-merger.d.ts.map