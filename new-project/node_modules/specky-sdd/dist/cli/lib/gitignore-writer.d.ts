export interface GitignoreResult {
    path: string;
    action: "created" | "updated" | "already-present";
    linesAdded: number;
}
/**
 * Ensure the Specky-managed block is present in .gitignore.
 * Idempotent: if the block already exists, replace it (to pick up path changes
 * across versions). Never touches other user-authored entries.
 */
export declare function writeGitignoreBlock(workspace: string, opts: {
    dryRun: boolean;
}): GitignoreResult;
//# sourceMappingURL=gitignore-writer.d.ts.map